using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IntellisenseTestert : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int redWang = 0;
        redWang += 1;
        redSwang += 1;

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
